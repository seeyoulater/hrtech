/** Number of cover letters the user is nudged to generate. */
export const GOAL = 5;

/** localStorage key for the persisted applications collection. */
export const STORAGE_KEY = 'hrtech.applications.v1';
