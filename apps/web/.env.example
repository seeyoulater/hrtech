# Optional. By default the web app calls /api/* on the same origin (or
# the Vite dev proxy forwards to http://localhost:3001 in dev). Set this
# only if you need to point the web app at a different API host —
# e.g. a deployed staging API.
VITE_API_URL=
