{
  "name": "hrtech",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @hrtech/web run dev",
    "dev:api": "pnpm --filter @hrtech/api run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "preview": "pnpm --filter @hrtech/web run preview"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
